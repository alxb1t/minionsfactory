# DEPRECATED. The gate is the root `Makefile`'s `gate` target; the mf-* skills and CI
# run `make gate`.
# Only the parked runner (`orchestrator/gate.py`) reads this array.
# It must match the `Makefile` recipe command for command until the runner moves.
# When the runner runs `make gate`, delete this file.
gate = [
  "uv sync --locked",
  "uv run ruff format --check .",
  "uv run ruff check .",
  "uv run ty check",
  "uv run pytest -q",
  "uv run python -m orchestrator specs check --strict",
]
