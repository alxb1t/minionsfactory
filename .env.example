# --- Obsidian vault path (the PM-side project folder Claude Code reads + edits) ---
# VAULT_PROJECT_DIR = the MinionsFactory project folder in the vault; the active plan is the
# highest-versioned implementation-plans/vX.Y_*implementation_plan.md (ignore archive/).
VAULT_PROJECT_DIR="/path/to/vault/Lab/MinionsFactory"
