# Local paths and secrets — gitignored, never committed. Copy to `.env`.
#
# No keys are required: the orchestrator reads nothing outside the repository it is
# pointed at. This file is scaffolding, kept so a target that later needs a local
# secret has the shape to declare it in.
